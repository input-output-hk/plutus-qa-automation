package io.iohk.utils;

public class Enums {

    public enum Runner {REMOTE, LOCALHOST}

    public enum Browser {CHROME, FIREFOX}

    public enum Environment {PROD, DEV_KRIS, DEV_DAVID}

    public enum SmartContract {CROWDFUNDING, GAME, MESSAGES, VESTING}

    public enum MainPageLinks {GETTING_STARTED, TUTORIAL, API, PRIVACY}
}
